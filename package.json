{
  "name": "backend",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build":"cd client && npm run build",
    "install-client":"cd client && npm install",
    "heroku-postbuild": "npm run install-client && npm run build",
    "start":"node app.js",
    "client2":"cd client && npm start",
    "dev": "concurrently -n 'server,clients' -c 'red,green' \"node app.js\" \"npm run client2\""
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "all": "^0.0.0",
    "bcrypt": "^4.0.1",
    "body-parser": "^1.19.0",
    "cors": "^2.8.5",
    "express": "^4.17.1",
    "mongoose": "^5.7.10",
    "nodemailer": "^6.4.6"
  }
}
